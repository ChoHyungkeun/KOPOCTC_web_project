<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>후기 목록</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        html, body {
            height: 100%;
            margin: 0;
            padding: 0;
        }

        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        main {
            flex: 1;
        }

        footer {
            flex-shrink: 0;
        }

        footer .container {
            background: transparent !important;
            border-radius: 0 !important;
        }
    </style>
</head>
<body>

{{>layouts/header}}

<main class="container my-4">
    <table class="table table-dark table-striped">
        <thead>
        <tr>
            <th style="width: 10%;">글번호</th>
            <th style="width: 70%;">제목</th>
            <th style="width: 20%;">작성자</th>
        </tr>
        </thead>
        <tbody>
        {{#articles}}
            <tr>
                <td>{{id}}</td>
                <td><a href="/text/{{id}}" class="text-info text-decoration-none">{{title}}</a></td>
                <td>{{writer}}</td>
            </tr>
        {{/articles}}
        </tbody>
    </table>

    <div class="text-center mt-3">
        {{#pageNumbers}}
            {{#isCurrentPage}}
                <span class="fw-bold text-primary mx-2">{{number}}</span>
            {{/isCurrentPage}}
            {{^isCurrentPage}}
                <a href="/drink/list?page={{numberMinusOne}}" class="mx-2">{{number}}</a>
            {{/isCurrentPage}}
        {{/pageNumbers}}
    </div>

    <div class="text-end mt-3 mb-5">
        <a href="/newtext" class="btn btn-primary">작성하기</a>
    </div>
</main>

{{>layouts/footer}}

</body>
</html>